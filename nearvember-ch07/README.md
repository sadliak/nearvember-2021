# NEARvember #7 (Elections Platform)

## Usage

* Install dependencies
  ```shell
  $ yarn
  ```

* Compile contract to WASM
  ```shell
  $ yarn build
  ```

* Deploy contract (described in root `README.md`)
